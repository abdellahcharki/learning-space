<%- include("../components/header.ejs") %>
<%- include("../components/navbar.ejs") %>

    <section class="sec-header">
      <div class="container">
        <div class="row">
          <div class="col-12">
            <h2 class="page-title"><%= c.title%></h2>
            <!-- <a href="/courses /new" class="btn act-btn float-end">new Cours</a> -->
            <div class="dropdown">
                <button class="btn btn-secondary float-end act-btn dropdown-toggle" type="button" data-bs-toggle="dropdown"  >  actions </button>
                <ul class="dropdown-menu">
                  <li><a class="dropdown-item"  href="/courses/<%= c.course.id %>/<%= c.id %>/edit"> Edit </a></li>
                  <li><a class="dropdown-item" href="#">Delete</a></li> 
                </ul>
              </div>
  
  
  
            <nav aria-label="breadcrumb"   >
                <ol class="breadcrumb">
                  <li class="breadcrumb-item"><a href="/"><i class="fas fa-home    "></i></a></li>
                  <li class="breadcrumb-item"><a href="/courses">Courses</a></li>
                  <li class="breadcrumb-item"><a href="/courses/<%= c.course.id %>"><%= c.course.name %></a></li>
                  <li class="breadcrumb-item active" aria-current="page"> <%= c.title%></li>
                </ol>
              </nav>
              <hr>                  
              <p class="create_datum"> <i class="fas fa-info-circle    "></i> <span class=" bold"><strong>Last updated</strong></span> <%= moment( c.updatedAt).format(' Do MMMM YYYY, HH:mm:ss ')  %></p>
  
        </div> 
        </div>
      </div>
    </section>

    <section class="container">
        <div class="row">

            <textarea class="d-none" type="hidden" id="body"><%- c.body %></textarea>
            <div class="col-12 content" id="content">
          
                
            </div> 

            
        </div>
    </section>

 
    <%- include("../components/editor.ejs") %>
    <%- include("../components/footer.ejs") %>


<script  type="module">
    const parser = new edjsParser();
    var data = JSON.parse(document.getElementById("body").value);
    var c  = parser.parse(data);
    document.getElementById("content").innerHTML =  myParser(data); 
 </script>
